## Requirements

You can find the requirements in the [REQUIREMENTS.md](./REQUIREMENTS.md) file.

## Running Playwright testing suite

You can run the Playwright tests with with:

```sh
npm install @playwright/test
npx playwright install
npx playwright test
```